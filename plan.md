# AI 项目重构通用计划

这份文件是跨项目复用的重构手册，不记录每个项目的流水账。项目级进度、接口报错、临时决策放到 `docs/refactor-log.md`，避免计划文件过长、难读，也减少后续上下文和 token 消耗。

## 0. 五个原则

1. 先想清楚再动手：明确目标、边界、风险、验证方式。
2. 先补安全网：至少保证能安装、能启动、能构建、能冒烟。
3. 小步快跑：每轮只迁一个清晰能力，频繁验证。
4. 先保功能不崩：行为兼容优先，代码质量随后提升。
5. 翻车及时复盘：把可复用经验沉淀成清单，而不是散在聊天里。

## 1. 启动摸底

- 技术栈：框架、构建工具、路由、状态管理、UI 库、请求库。
- 入口链路：应用入口、根组件、路由守卫、状态入口、请求拦截器、运行时配置。
- 业务地图：按路由、菜单、页面目录、接口域拆模块。
- 高风险区：登录鉴权、请求加解密、运行时配置、全局状态、缓存、WebSocket、上传下载、外部系统。
- 编码风险：旧项目若存在 GBK/乱码，禁止整文件格式化，优先局部迁移或重写为 UTF-8。

## 2. 安全网

- 构建安全网：每轮至少跑 `npm run build` 或对应构建命令。
- 登录安全网：验证 token 保存、刷新恢复用户、退出清理状态。
- 请求安全网：验证 baseURL、apiVersion、白名单、加密、响应解密。
- 页面安全网：验证菜单、页签、刷新、关闭、外链、权限入口。
- 回滚安全网：每轮前后检查 `git status --short`，不混入无关改动。

## 3. 推荐迁移顺序

1. 新项目骨架：路由、状态、样式、请求层、布局。
2. 运行时配置：同步后端地址和部署期配置方式。
3. 请求兼容层：复刻旧项目 baseURL、token、加密、响应解密。
4. 登录与权限：普通登录、SSO、用户信息、菜单/按钮权限。
5. 主框架首页：菜单、用户、通知、页签、外链 iframe、退出。
6. 低风险业务页：字典、简单列表、配置页。
7. 核心业务页：按领域拆服务、状态、组件、工具函数。
8. 清理兼容垫片：在功能稳定后统一命名、抽象和测试。

## 4. 每轮执行模板

### 目标

- 本轮迁移什么能力：
- 明确不做什么：

### 影响范围

- 页面：
- 接口：
- 状态：
- 本地存储：

### 验证

- 构建命令：
- 关键路径：
- 失败回退：

### 复盘

- 可复用经验：
- 下轮风险：
- 是否需要更新本计划：

## 5. 文档策略

- `plan.md`：只写跨项目可复用的原则、顺序、检查清单。
- `docs/refactor-log.md`：写当前项目的迁移日志、问题、接口细节。
- `docs/refactor-plan.md`：写当前项目阶段性计划，可比 `plan.md` 更具体。
- 不把大量代码路径、接口响应、错误堆栈塞进 `plan.md`，否则会降低复用性，也会增加后续阅读和 token 成本。

## 6. 停手条件

- 构建失败且原因不明确。
- 核心路径行为与旧项目明显不一致。
- 一个改动开始牵出多个业务域。
- 发现旧项目有隐性运行时依赖。
- 工作区出现大量无关变更。

## Realtime Checklist

- 迁移 WebSocket 前先确认：连接地址、账号拼接方式、心跳间隔、重连上限、退出时是否阻止重连。
- 实时消息先按类型分流：普通日志、徽章更新、徽章已读、公告通知、其它业务事件分别处理，避免把 CTI/工单等高风险能力提前混入首页。
- Badge/通知类实时刷新要保留兜底接口：连接成功刷新一次，消息到达局部刷新，弹窗打开时刷新列表，弹窗关闭时只刷新未读数。
- 每轮实时能力迁移后必须验证：构建通过、登录后不会阻塞首页、退出会关闭 socket 和定时器、断线重连不会重复创建多个连接。
